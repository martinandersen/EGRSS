notifications:
  email: false

matrix:
  include:
#    - os: linux
#      dist: bionic
#      language: c
    - os: osx
      language: c

before_script:
  - cmake --version

script:
  - cmake -S . -B build
  - cmake --build build
